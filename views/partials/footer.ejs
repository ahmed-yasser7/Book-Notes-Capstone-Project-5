  </div><!-- /.container -->
</main>

<footer>
  <div class="footer-inner">
    <span class="footer-logo">My Bookshelf</span>
    <span>© <%= new Date().getFullYear() %> Ahmed Yasser. All rights reserved.</span>
    <span>Book covers powered by <a href="https://openlibrary.org" target="_blank" style="color: var(--accent); text-decoration: none;">Open Library</a></span>
  </div>
</footer>

<script>
  // Dark mode toggle
  const toggle = document.getElementById('themeToggle');
  const html = document.documentElement;

  // Load saved preference
  const saved = localStorage.getItem('theme') || 'light';
  html.setAttribute('data-theme', saved);

  toggle.addEventListener('click', () => {
    const current = html.getAttribute('data-theme');
    const next = current === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', next);
    localStorage.setItem('theme', next);
  });
</script>

</body>
</html>
